from models import create_tables, drop_tables
from services import *

# create_tables()
# drop_tables()

#create a new user
# new_user = create_user(name="Alice", email="alice@wonderland.com")
# print(f"Created User: {new_user}")

# new_post = create_post(user_id=1, title="My First Post", content="This is the content of my first post.")
# print(f"Created Post: {new_post}")

# fetched_user = get_user(user_id=1)
# print(f"Fetched User: {fetched_user}")

# fetched_post = get_post(post_id=1)
# print(f"Fetched Post: {fetched_post}")

# posts_by_user = get_posts_by_user(user_id=1)
# print(f"Posts by User 1: {posts_by_user}")

# delete_status = delete_post(post_id=1)
# print(f"Deleted Post 1: {delete_status}")

# delete_status = delete_posts_by_user(user_id=1)
# print(f"Deleted Posts by User 1: {delete_status}")

# delete_status = delete_user(user_id=1)
# print(f"Deleted User 1: {delete_status}")