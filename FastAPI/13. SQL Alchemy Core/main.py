from services import create_user, create_post, delete_user, get_user, update_user, get_posts_by_user, delete_post, update_post

# create_tables()
# drop_tables()

# create_user(name="John Doe", email="john.doe@example.com")
# create_post(title="First Post", content="This is the content of the first post", user_id=1)
delete_user(user_id=1)